# ProdCollab — Complete Todo List
### From broken to production ready. Simple to hardest. Do in order.

---

## PHASE 1 — CLEANUP
> Remove dead weight before touching anything else. No new code yet.

- [ ] **1.1** Delete all commented-out old code in `App.jsx` — the entire first version is sitting there doing nothing
- [ ] **1.2** Delete all commented-out old code in `preload.js` — same problem
- [ ] **1.3** Delete all commented-out old code in `authController.js`
- [ ] **1.4** Delete `useAuth.js` hook — it uses the old session-based auth and is no longer used anywhere
- [ ] **1.5** Delete `api.js` utility — it references old endpoints that no longer exist
- [ ] **1.6** In `server.js` — remove `require('body-parser')` and `app.use(bodyParser.json())`. Replace with `app.use(express.json())` and `app.use(express.urlencoded({ extended: true }))` — bodyParser is deprecated since Express 4.16
- [ ] **1.7** In `server.js` — remove unused imports and dead route references
- [ ] **1.8** In `Settings.jsx` — the entire file is hardcoded fake data with no real functionality. Mark clearly as UI-only placeholder or delete and rebuild later
- [ ] **1.9** Audit all `console.log` and `console.error` statements — remove or replace with a proper logger before production

---

## PHASE 2 — BUG FIXES
> Fix what is currently broken without changing the architecture yet.

- [ ] **2.1** Fix the projectId type mismatch bug — the root cause of the folder re-selection issue
  - In `main.js` Chokidar watcher — `projectId` is stored and sent as a String
  - In Dashboard, Projects, Collaboration — `p.id` from MySQL is a Number
  - `5 === "5"` is `false` in JavaScript — so every `projects.find()` fails silently
  - Fix: replace every `p.id === projectId` with `String(p.id) === String(projectId)` across all three pages
  - This affects: `handleFileChange`, `handlePushChanges`, `handleCheckChanges`, `handleDeleteProject`, `handleLeaveProject`

- [ ] **2.2** Fix folder path persistence across sessions
  - Currently `ensureFolderPath` re-prompts even when folder was previously saved
  - Root cause: the bug in 2.1 causes project lookup to fail, making it look like no folder is saved
  - Once 2.1 is fixed — test this is resolved. If not, add explicit logging to `get-folder-path` IPC handler to confirm the store is reading correctly

- [ ] **2.3** Fix the double `getCollaboratedProjects` export in `projectController.js`
  - The function is defined twice — once in the middle of the file and once at the bottom
  - The second definition overwrites the first silently
  - Delete the duplicate — keep only one

- [ ] **2.4** Fix push failing silently when `filesToUpload.length === 0`
  - Currently logs a message but still calls `pushFilesToGitHub` with an empty array
  - Should return early with a clear user-facing message
  - Add proper guard: `if (filesToUpload.length === 0) return res.json({ message: 'Nothing new to push', filesUploaded: 0 })`

- [ ] **2.5** Fix `pushRes.json()` called twice on failure in Dashboard and Projects push handlers
  - `const pushData = await pushRes.json()` is called once
  - Then in the `else` block: `const errorData = await pushRes.json()` is called again
  - A response body can only be consumed once — the second call returns empty
  - Fix: use `pushData` in both the success and failure branches

- [ ] **2.6** Fix Toast receiving an Error object instead of a string in several places
  - `setToast({ type: 'error', message: error })` — `error` is an object not a string
  - Fix: `setToast({ type: 'error', message: error.message })`
  - Affects multiple catch blocks across Dashboard, Projects, Collaboration

---

## PHASE 3 — DATABASE MIGRATION
> Move from Aiven MySQL to Supabase PostgreSQL. Do this before touching auth.

- [ ] **3.1** Create a new Supabase project specifically for ProdCollab at supabase.com

- [ ] **3.2** Create the tables in Supabase using the SQL editor — PostgreSQL syntax differs from MySQL:
  ```sql
  -- Users table (Supabase Auth handles passwords — this stores profile data)
  CREATE TABLE users (
    id UUID PRIMARY KEY REFERENCES auth.users(id),
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    avatar_url TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
  );

  -- Projects table
  CREATE TABLE projects (
    id BIGSERIAL PRIMARY KEY,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    repo_name TEXT NOT NULL,
    repo_url TEXT,
    description TEXT,
    visibility TEXT DEFAULT 'private' CHECK (visibility IN ('public', 'private')),
    file_paths JSONB DEFAULT '{"individualFiles": [], "folders": []}',
    has_changes BOOLEAN DEFAULT FALSE,
    share_token TEXT UNIQUE,
    last_pulled_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
  );

  -- Project collaborators table
  CREATE TABLE project_collaborators (
    id BIGSERIAL PRIMARY KEY,
    project_id BIGINT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    role TEXT DEFAULT 'collaborator' CHECK (role IN ('collaborator', 'admin')),
    local_path TEXT,
    joined_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(project_id, user_id)
  );
  ```

- [ ] **3.3** Enable Row Level Security (RLS) on all tables in Supabase dashboard
  - Users can only read/write their own data
  - Collaborators can read projects they belong to
  - This replaces manual auth checks in every controller

- [ ] **3.4** In your server — install Supabase client:
  ```bash
  npm install @supabase/supabase-js
  ```

- [ ] **3.5** Remove `mysql2` package:
  ```bash
  npm uninstall mysql2
  ```

- [ ] **3.7** Create `server/config/supabase.js`:
  ```javascript
  const { createClient } = require('@supabase/supabase-js')

  const supabase = createClient(
    process.env.SUPABASE_URL,
    process.env.SUPABASE_SERVICE_KEY // service key for server-side, bypasses RLS
  )

  module.exports = supabase
  ```

- [ ] **3.8** Update `.env` — remove all MySQL variables, add Supabase variables:
  ```
  SUPABASE_URL=
  SUPABASE_SERVICE_KEY=
  SUPABASE_ANON_KEY=
  ```

- [ ] **3.9** Rewrite `authController.js` to use Supabase instead of MySQL pool
  - Replace all `pool.promise().getConnection()` calls
  - Replace all `connection.execute()` with `supabase.from('table').select/insert/update`
  - Remove all `connection.release()` calls — not needed with Supabase client

- [ ] **3.10** Rewrite `projectController.js` to use Supabase instead of MySQL pool
  - Same pattern — replace every pool call with Supabase client call
  - JSONB in PostgreSQL means you can query inside `file_paths` directly without parsing

3.11 make sure the ennd point are well

---

## PHASE 4 — AUTH OVERHAUL
> Replace GitHub OAuth with email/password for login and username/email/password for creating account. Remove GitHub account requirement from users.

- [ ] **4.1** Create a dedicated ProdCollab GitHub account (e.g. `@prodcollab-app`)
  - This is the account that will own ALL repos
  - Generate a Personal Access Token with `repo` scope
  - Add to `.env`: `PRODCOLLAB_GITHUB_TOKEN=`

- [ ] **4.2** Create `server/config/github.js` — single shared Octokit instance:
  ```javascript
  const { Octokit } = require('@octokit/rest')

  const octokit = new Octokit({
    auth: process.env.PRODCOLLAB_GITHUB_TOKEN
  })

  const GITHUB_OWNER = 'prodcollab-app' // your fixed account name

  module.exports = { octokit, GITHUB_OWNER }
  ```

- [ ] **4.3** Remove `github_tokens` table from Supabase entirely — no longer needed

- [ ] **4.4** Update all `projectController.js` functions:
  - Remove `const [users] = await connection.execute('SELECT... github_tokens...')`
  - Remove `const octokit = new Octokit({ auth: user.access_token })`
  - Remove `const { data: githubUser } = await octokit.users.getAuthenticated()`
  - Replace with `const { octokit, GITHUB_OWNER } = require('../config/github')`
  - Replace all `owner: githubUser.login` with `owner: GITHUB_OWNER`

- [ ] **4.5** Enable Supabase Auth email/password provider in Supabase dashboard
  - Authentication → Providers → Email → Enable

- [ ] **4.6** Rewrite auth routes — three simple endpoints:
  ```
  POST /api/auth/signup    → email, password, username
  POST /api/auth/login     → email, password
  POST /api/auth/logout    → clear session
  GET  /api/auth/me        → return current user
  ```

- [ ] **4.7** Rewrite `authController.js` signup:
  ```javascript
  exports.signup = async (req, res) => {
    const { email, password, username } = req.body
    
    // Create auth user in Supabase Auth
    const { data, error } = await supabaseAdmin.auth.admin.createUser({
      email, password, email_confirm: true
    })
    
    if (error) return res.status(400).json({ error: error.message })
    
    // Create profile in users table
    await supabase.from('users').insert({
      id: data.user.id,
      email,
      username
    })
    
    res.json({ message: 'Account created' })
  }
  ```

- [ ] **4.8** Rewrite `authController.js` login — return httpOnly cookie, not localStorage token:
  ```javascript
  exports.login = async (req, res) => {
    const { email, password } = req.body
    
    const { data, error } = await supabase.auth.signInWithPassword({ email, password })
    
    if (error) return res.status(401).json({ error: 'Invalid credentials' })
    
    // Set httpOnly cookie instead of returning token in body
    res.cookie('prodcollab_token', data.session.access_token, {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'strict',
      maxAge: 60 * 60 * 1000 // 1 hour
    })
    
    res.cookie('prodcollab_refresh', data.session.refresh_token, {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'strict',
      maxAge: 7 * 24 * 60 * 60 * 1000 // 7 days
    })
    
    res.json({ user: data.user })
  }
  ```

- [ ] **4.9** Install `cookie-parser` on server:
  ```bash
  npm install cookie-parser
  ```
  Add to `server.js`: `app.use(require('cookie-parser')())`

- [ ] **4.10** Rewrite `authMiddleware.js` — read from cookie not Authorization header:
  ```javascript
  exports.verifyToken = async (req, res, next) => {
    const token = req.cookies.prodcollab_token
    
    if (!token) return res.status(401).json({ error: 'Not authenticated' })
    
    const { data, error } = await supabase.auth.getUser(token)
    
    if (error) return res.status(401).json({ error: 'Invalid session' })
    
    req.userId = data.user.id
    req.user = data.user
    next()
  }
  ```

- [ ] **4.11** Delete `LoginPage.jsx` — the GitHub OAuth one

- [ ] **4.12** Build new `LoginPage.jsx` — email and password only for login and usename email and password for signup:
  - Email field
  - Password field
  - Login button
  - Link to signup page
  - No GitHub button anywhere

- [ ] **4.13** Build new `SignupPage.jsx`:
  - Username field
  - Email field
  - Password field
  - Confirm password field
  - Create Account button

- [ ] **4.14** Update `App.jsx`:
  - Remove all `localStorage` token logic
  - Remove GitHub OAuth callback handling
  - Remove `isTokenExpired` function
  - Remove `refreshAccessToken` function
  - Replace with `checkAuth` that calls `/api/auth/me` — if cookie exists and is valid, user is in. If not, go to login.
  - Session is now managed entirely by cookies and Supabase

- [ ] **4.15** Update all fetch calls across Dashboard, Projects, Collaboration:
  - Remove all `headers: { Authorization: Bearer ${jwtToken} }` — cookies send automatically
  - Add `credentials: 'include'` to every fetch call so cookies are sent cross-origin
  - Remove `jwtToken` prop being passed down from App to every page

- [ ] **4.16** Update `forge.config.js` — remove GitHub OAuth protocol handler since it's no longer needed:
  - Remove `protocols` array from `packagerConfig`
  - Remove `app.setAsDefaultProtocolClient('prodcollab')` from `main.js`
  - Remove all OAuth protocol handling in `main.js` (the `open-url`, `second-instance` OAuth sections)
  - Remove OAuth navigation interceptors in `createWindow`

---

## PHASE 5 — REPLACE FILE TRANSFER MECHANISM
> Replace Octokit blob API with simple-git. This fixes speed completely.

- [ ] **5.1** Install `simple-git` in your Electron frontend:
  ```bash
  cd frontend
  npm install simple-git
  ```

- [ ] **5.2** In `main.js` — add git initialization when a project folder is selected:
  ```javascript
  const simpleGit = require('simple-git')

  ipcMain.handle('init-git', async (_, { folderPath, repoUrl }) => {
    const git = simpleGit(folderPath)
    const isRepo = await git.checkIsRepo()
    
    if (!isRepo) {
      await git.init()
      await git.addRemote('origin', repoUrl)
    }
    
    return { success: true }
  })
  ```

- [ ] **5.3** Add git push IPC handler in `main.js`:
  ```javascript
  ipcMain.handle('git-push', async (_, { folderPath, message, username }) => {
    const git = simpleGit(folderPath)
    
    await git.add('.')
    await git.commit(message || `Update by ${username}`)
    await git.push('origin', 'main')
    
    return { success: true }
  })
  ```

- [ ] **5.4** Add git pull IPC handler in `main.js`:
  ```javascript
  ipcMain.handle('git-pull', async (_, { folderPath }) => {
    const git = simpleGit(folderPath)
    await git.pull('origin', 'main')
    return { success: true }
  })
  ```

- [ ] **5.5** Add git clone IPC handler in `main.js` — for when collaborator joins:
  ```javascript
  ipcMain.handle('git-clone', async (_, { repoUrl, folderPath }) => {
    await simpleGit().clone(repoUrl, folderPath)
    return { success: true }
  })
  ```

- [ ] **5.6** Add git log IPC handler — for version history:
  ```javascript
  ipcMain.handle('git-log', async (_, { folderPath }) => {
    const git = simpleGit(folderPath)
    const log = await git.log({ maxCount: 50 })
    return log.all
  })
  ```

- [ ] **5.7** Expose all new git handlers in `preload.js`:
  ```javascript
  initGit: (data) => ipcRenderer.invoke('init-git', data),
  gitPush: (data) => ipcRenderer.invoke('git-push', data),
  gitPull: (data) => ipcRenderer.invoke('git-pull', data),
  gitClone: (data) => ipcRenderer.invoke('git-clone', data),
  gitLog: (folderPath) => ipcRenderer.invoke('git-log', folderPath),
  ```

- [ ] **5.8** Simplify server push endpoint — it no longer handles file bytes:
  ```javascript
  // New push endpoint — just records the push in DB
  exports.recordPush = async (req, res) => {
    const { projectId } = req.params
    const { commitMessage, fileCount } = req.body
    
    await supabase.from('projects')
      .update({ has_changes: false, updated_at: new Date() })
      .eq('id', projectId)
    
    res.json({ message: 'Push recorded', fileCount })
  }
  ```

- [ ] **5.9** Simplify server pull endpoint — it just returns the repo clone URL:
  ```javascript
  exports.getPullInfo = async (req, res) => {
    const { projectId } = req.params
    
    const { data: project } = await supabase
      .from('projects')
      .select('repo_url, repo_name')
      .eq('id', projectId)
      .single()
    
    await supabase.from('projects')
      .update({ last_pulled_at: new Date() })
      .eq('id', projectId)
    
    res.json({ repoUrl: project.repo_url })
  }
  ```

- [ ] **5.10** Remove `multer` from server — no longer uploading files through server:
  ```bash
  npm uninstall multer
  ```
  Remove all `upload.array()` middleware from project routes

- [ ] **5.11** Remove the `uploads/` temp folder from server — no longer needed

- [ ] **5.12** Delete `pushFilesToGitHub` helper function — replaced by simple-git

- [ ] **5.13** Delete `getAllRepoFiles` helper function — replaced by simple-git

- [ ] **5.14** Delete `fileToBase64` helper function — no longer needed

- [ ] **5.15** Update Dashboard and Projects push handler to use new git flow:
  ```javascript
  const handlePushChanges = async (projectId) => {
    const folderPath = await window.electronAPI.getFolderPath(projectId)
    
    await window.electronAPI.gitPush({
      folderPath,
      message: `Update by ${user.username}`,
      username: user.username
    })
    
    // Tell server the push happened
    await fetch(`/api/projects/${projectId}/push`, {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ commitMessage: `Update by ${user.username}` })
    })
    
    setToast({ type: 'success', message: 'Pushed successfully' })
  }
  ```

- [ ] **5.16** Update join project flow to use git clone:
  ```javascript
  // After user selects folder and pastes share link
  const { repoUrl } = await fetch(`/api/projects/join`, { ... }).then(r => r.json())
  await window.electronAPI.gitClone({ repoUrl, folderPath })
  ```

---

## PHASE 6 — AUTO PUSH AND SILENT SYNC
> Remove all manual Check Changes and Push buttons. Make it invisible.

- [ ] **6.1** In `main.js` — add debounced auto-push timer to the file watcher:
  ```javascript
  const pushTimers = new Map()
  const PUSH_DELAY = 10 * 60 * 1000 // 10 minutes

  // Inside watcher event handlers:
  watcher.on('change', (filePath) => {
    const pid = String(projectId)
    
    // Clear existing timer
    if (pushTimers.has(pid)) clearTimeout(pushTimers.get(pid))
    
    // Notify UI — dot goes orange
    notifyAll('file-changed', { projectId: pid, event: 'change', path: filePath })
    
    // Schedule auto-push
    const timer = setTimeout(() => {
      notifyAll('auto-push-ready', { projectId: pid })
    }, PUSH_DELAY)
    
    pushTimers.set(pid, timer)
  })
  ```

- [ ] **6.2** Add IPC handler for immediate manual push override:
  ```javascript
  ipcMain.handle('push-now', async (_, { projectId }) => {
    // Clear the pending timer
    if (pushTimers.has(String(projectId))) {
      clearTimeout(pushTimers.get(String(projectId)))
      pushTimers.delete(String(projectId))
    }
    notifyAll('auto-push-ready', { projectId: String(projectId) })
    return { success: true }
  })
  ```

- [ ] **6.3** Expose in `preload.js`:
  ```javascript
  pushNow: (projectId) => ipcRenderer.invoke('push-now', { projectId }),
  onAutoPushReady: (callback) => {
    ipcRenderer.on('auto-push-ready', (_, data) => callback(data))
  }
  ```

- [ ] **6.4** In Dashboard — listen for `auto-push-ready` and trigger push silently:
  ```javascript
  useEffect(() => {
    window.electronAPI.onAutoPushReady(async ({ projectId }) => {
      await handlePushChanges(projectId) // same function, just called automatically
    })
  }, [])
  ```

- [ ] **6.5** Add stems and exports subfolder auto-creation when project is created:
  ```javascript
  // In main.js after folder is saved
  ipcMain.handle('setup-project-folder', async (_, { folderPath }) => {
    await fs.mkdir(path.join(folderPath, 'stems'), { recursive: true })
    await fs.mkdir(path.join(folderPath, 'exports'), { recursive: true })
    return { success: true }
  })
  ```

- [ ] **6.6** Call `setup-project-folder` after project creation and after joining

- [ ] **6.7** Remove "Check Changes" button from ProjectCard component entirely

- [ ] **6.8** Remove `handleCheckChanges` function from Dashboard, Projects, Collaboration

- [ ] **6.9** Remove `detectFileChanges` endpoint from server — no longer needed

- [ ] **6.10** Replace the removed buttons with a subtle status indicator in ProjectCard:
  ```
  🟢 Synced          ← no pending changes
  🟡 Changes pending  ← watcher detected changes, timer running
  🔵 Pushing...      ← push in progress
  🔴 Push failed     ← error state
  ```

- [ ] **6.11** Add "Push now" right-click option in system tray for each watched project

---

## PHASE 7 — REAL-TIME NOTIFICATIONS
> Collaborators get notified instantly when something is pushed. No polling.

- [ ] **7.1** Enable Supabase Realtime on the `projects` table in Supabase dashboard
  - Database → Replication → Tables → enable `projects`

- [ ] **7.2** In frontend — subscribe to realtime changes on projects the user belongs to:
  ```javascript
  useEffect(() => {
    const subscription = supabase
      .channel('project-changes')
      .on(
        'postgres_changes',
        {
          event: 'UPDATE',
          schema: 'public',
          table: 'projects',
          filter: `id=in.(${userProjectIds.join(',')})`
        },
        (payload) => {
          const project = payload.new
          setToast({
            type: 'info',
            message: `${project.last_pushed_by} just pushed changes to ${project.repo_name}`
          })
          // Auto-pull if user has auto-sync enabled
        }
      )
      .subscribe()

    return () => supabase.removeChannel(subscription)
  }, [userProjectIds])
  ```

- [ ] **7.3** Add `last_pushed_by` column to projects table:
  ```sql
  ALTER TABLE projects ADD COLUMN last_pushed_by TEXT;
  ```

- [ ] **7.4** Update push endpoint to write `last_pushed_by` on every push:
  ```javascript
  await supabase.from('projects')
    .update({ 
      has_changes: false, 
      last_pushed_by: req.user.email,
      updated_at: new Date() 
    })
    .eq('id', projectId)
  ```

- [ ] **7.5** Add auto-pull option in settings — when a collaborator pushes, user can have it pull automatically into their local folder

---

## PHASE 8 — VERSION HISTORY UI
> Every file shows who touched it and when. Producers can restore any version.

- [ ] **8.1** Build `VersionHistory.jsx` component:
  - Show list of commits from `git-log`
  - Each commit: username, timestamp, short message, files changed
  - Restore button per commit

- [ ] **8.2** Add version history panel to ProjectCard — click to expand

- [ ] **8.3** Add `git-restore` IPC handler in `main.js`:
  ```javascript
  ipcMain.handle('git-restore', async (_, { folderPath, commitSha }) => {
    const git = simpleGit(folderPath)
    await git.checkout(commitSha)
    return { success: true }
  })
  ```

- [ ] **8.4** Expose in preload:
  ```javascript
  gitRestore: (data) => ipcRenderer.invoke('git-restore', data)
  ```

---

## PHASE 9 — UI POLISH
> Make it look and feel like a real product.

- [ ] **9.1** Design and build proper system tray icon with status indicator
  - Green: all synced
  - Orange: changes pending
  - Red: push failed

- [ ] **9.2** Build system tray context menu:
  - Project name and status per project
  - "Push now" option per project
  - "Open ProdCollab" to bring window to front
  - "Quit"

- [ ] **9.3** Build proper onboarding flow for new users:
  - Step 1: Welcome screen
  - Step 2: Create account
  - Step 3: Create first project or join one
  - Step 4: Folder selection with explanation
  - Step 5: "You're ready — go make music"

- [ ] **9.4** Build Settings page with real functionality:
  - Profile: username, email, avatar
  - Sync settings: auto-push delay (5 min / 10 min / 30 min / manual only)
  - Notification preferences
  - Connected projects and their folder paths
  - Danger zone: delete account

- [ ] **9.5** Fix loading states — every async action needs a visual indicator

- [ ] **9.6** Add empty states with clear calls to action on every page

- [ ] **9.7** Add error boundary component — if something crashes don't show white screen

- [ ] **9.8** Make all modals closable with Escape key

- [ ] **9.9** Add keyboard shortcuts: Cmd/Ctrl+P for push now, Cmd/Ctrl+R for refresh

---

## PHASE 10 — PRODUCTION READINESS
> Everything needed before real users touch it.

- [ ] **10.1** Add proper server-side input validation on every endpoint
  - Project name length limits
  - Email format validation
  - File path sanitization
  - No SQL injection possible (Supabase client handles this but verify)

- [ ] **10.2** Add rate limiting to auth endpoints:
  ```bash
  npm install express-rate-limit
  ```
  ```javascript
  const limiter = rateLimit({
    windowMs: 15 * 60 * 1000,
    max: 10 // 10 login attempts per 15 minutes
  })
  app.use('/api/auth/login', limiter)
  ```

- [ ] **10.3** Add proper error logging — replace all `console.error` with a real logger:
  ```bash
  npm install winston
  ```

- [ ] **10.4** Set up environment configs for development and production:
  - `server/.env.development`
  - `server/.env.production`
  - Never commit either to git

- [ ] **10.5** Add `.gitignore` to server folder:
  ```
  node_modules/
  .env
  .env.development
  .env.production
  uploads/
  logs/
  ```

- [ ] **10.6** Set up GitHub Actions CI — run on every push to main:
  - Install dependencies
  - Run lint
  - Run basic tests
  - Block merge if failing

- [ ] **10.7** Write basic tests for critical endpoints:
  - Signup creates user
  - Login returns cookie
  - Unauthenticated request returns 401
  - Create project creates GitHub repo
  - Join project with valid token succeeds
  - Join project with invalid token returns 404

- [ ] **10.8** Add CORS config for production domain:
  ```javascript
  app.use(cors({
    origin: process.env.NODE_ENV === 'production'
      ? 'app://.' // Electron production
      : 'http://localhost:9000',
    credentials: true
  }))
  ```

- [ ] **10.9** Set up server deployment — Railway, Render, or Fly.io:
  - Free tier sufficient until real user load
  - Add all env variables to deployment dashboard
  - Set up auto-deploy from GitHub main branch

- [ ] **10.10** Test full packaged `.exe` build end to end before any users:
  - Signup flow
  - Create project
  - Select folder
  - Make a change in the folder
  - Verify auto-push fires after timer
  - Share link
  - Second account joins
  - Pull files
  - Verify files are in correct folder

- [ ] **10.11** Write a clear README for the repo:
  - What ProdCollab is
  - How to set up for development
  - Environment variables needed
  - How to build for production

- [ ] **10.12** Set up a proper domain for the server and update all hardcoded `localhost:5000` references to use an environment variable:
  ```javascript
  const API_URL = process.env.API_URL || 'http://localhost:5000'
  ```

---

## SUMMARY — COUNT

| Phase | Description | Items |
|-------|------------|-------|
| 1 | Cleanup | 9 |
| 2 | Bug fixes | 6 |
| 3 | DB migration to Supabase | 11 |
| 4 | Auth overhaul | 16 |
| 5 | Replace file transfer (simple-git) | 16 |
| 6 | Auto push and silent sync | 11 |
| 7 | Real-time notifications | 5 |
| 8 | Version history UI | 4 |
| 9 | UI polish | 9 |
| 10 | Production readiness | 12 |
| **Total** | | **99** |

---

## THE ORDER TO WORK IN

Do phases in order. Never skip ahead.

Phase 1 and 2 first — clean code catches bugs faster.
Phase 3 before Phase 4 — get the database right before touching auth.
Phase 4 before Phase 5 — auth must work before testing file transfers.
Phase 5 before Phase 6 — auto-push needs the git layer working first.
Phase 6 before Phase 7 — notifications are useless without auto-push.
Phase 7 before Phase 8 — stabilize sync before building history.
Phase 8 and 9 can run in parallel — UI and history are independent.
Phase 10 last — production readiness comes after everything works.

---

*ProdCollab — GitHub for Music Producers.*  
*Built so producers never have to think about version control.*