Build both Login and Sign Up pages using this layout:

Use a full-screen dark auth layout split into **two equal vertical sections**. The **left section is a minimal brand/identity panel**: place our existing logo near the upper-left, then use the content from `information.txt` as a large editorial statement in the main area. The text should feel prominent but remain visually secondary to the logo. Keep this side spacious with strong negative space.

The **right section contains the auth interface** inside a slightly contrasting bordered/rounded panel with generous internal padding. Put a small secondary action/link in the top-right (e.g. Create account / Login), then a large **Login/Sign Up heading**, followed by clean horizontally arranged form fields where appropriate. Use subtle bottom borders/dividers on inputs rather than heavy boxes. Place the primary submit action as a **small circular button near the bottom-right** of the form panel.

Use our existing **colours, typography, border treatment, spacing, radius and UI tokens** instead of copying the reference's visual styling. Keep the composition asymmetric, spacious, minimal and premium. Reuse this exact structure for both auth pages, changing only the form content and relevant links.
